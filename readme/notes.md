# css selectors 
- `section.allElems` means uss section ko target kro jiski class `allElems` hai 

- `section .allElems` means uss elem ko target kro jo ki section k andhar allElmes ki class  hai

# queryselectorAll 
- >its work is to find similar elems(having same id,class) in html 

# nodelist 

* >it is a kind of array or similar to array but not exactly array